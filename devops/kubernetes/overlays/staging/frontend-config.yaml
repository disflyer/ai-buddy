apiVersion: networking.gke.io/v1beta1
kind: FrontendConfig
metadata:
  name: websocket-frontend-config
spec:
  # 启用HTTPS重定向
  redirectToHttps:
    enabled: false
  # 配置SSL策略
  sslPolicy: gke-ingress-ssl-policy 